# MovieApp Codex guidance

Apply the project skills stored in this repository:

- For implementation, refactoring, debugging, architecture, tests, concurrency, networking, storage, security, or code review, use `$ios-staff-engineer` from `.agents/skills/ios-staff-engineer`.
- For visual design, UIKit screen work, Figma handoff, design systems, motion, accessibility, or visual QA, use `$premium-ios-design` from `.agents/skills/premium-ios-design`.
- For UI feature work, use both skills in this order: establish the design and states with `premium-ios-design`, then implement and verify the complete change with `ios-staff-engineer`.

Read each selected `SKILL.md` completely. Load only the reference files it directs you to. Project conventions and the user's explicit instructions take priority over generic preferences. Treat measurements, rendered screenshots, tests, and compiler diagnostics as stronger evidence than aesthetic or architectural assertions.

Do not edit generated TMDB API code directly. Preserve unrelated working-tree changes. Run relevant tests and report verification honestly.
